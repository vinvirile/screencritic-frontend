import styled from 'styled-components'

export const InputGroupBox = styled.div`
  margin: 1.8rem 0;

  input[type='text'],
  input[type='email'],
  input[type='password'] {
    width: 60%;
    font-size: 1.8rem;
  }
`
